import React from 'react';

import styles from './Login.module.scss';
interface Props {}

export const Login = (props: Props) => {
  return <div className={styles['container']}>I am login page</div>;
};
